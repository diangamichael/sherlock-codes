/**
 *  TODO Write a function called sortString that takes a string of letters and returns a string with the letters sorted in alphabetical order.
 */

function sortString(str) {
  return str.split('').sort().join('');
}
